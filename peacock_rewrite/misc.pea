export class Highlight < Div =
  color is_active =
    if is_active
      "#fdff328f"
    else 
      "unset"
    end
  style props = "
    display: inline;
    background-color: #{color props[:active]};
  "
